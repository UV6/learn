## 有没有用状态机？
- 子 Agent 后台运行状态：`running`、`completed`、`failed`。由 `TaskManager` 管理，关注“这次子 Agent 执行是否结束、结果是什么”。
    
- Team 共享任务状态：`pending`、`in_progress`、`completed`、`failed`。由共享任务看板管理，关注“协作任务是否可开始、由谁负责、依赖是否已完成”。