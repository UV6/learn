- [ ] 写一个测试的 hook，
- [x] 注意 hook 的触发位置，其实主要是工具触发前和触发后

```
hooks: 
- id: block-json-write 
  event: pre_tool_use 
  if: 'tool == "WriteFile" && args.file_path =~ /\.json$/' 
  action: 
	  type: command 
	  command: 'echo "禁止直接写入 JSON 文件，请使用专用工具"' 
  reject: true
```