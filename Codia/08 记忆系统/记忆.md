指令文件+会话持久化+自动记忆
#### 指令文件

三层指令文件
- <project-root/MEWCODE.md
	- 存放直接、显式的规则
- <project-root/.mewcode/MEWCODE.md
	 - 存放项目中自己私有的规则
- ~/.mewcode/MEWCODE.md
	- 存放用户级长期偏好和通用工作方式

@include
	项目很大的话，指令文件很多，可以通过该指令按条件引入，CODIA.md 只是作为汇总入口
#### 会话持久化

JSONL 格式

存放位置

会话管理器

恢复会话
	逐行解析 jsonl，验证完整性，检查 token 量 (如果超了就压缩)，时间跨度提示


#### 自动记忆

四类记忆

存放位置

提取+去重

记忆加载

